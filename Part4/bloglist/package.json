{
  "name": "bloglist",
  "version": "0.0.1",
  "description": "Blog List Application for Helsinki Full Stack MOOC",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "watch": "cross-env NODE_ENV=development nodemon index.js",
    "clear-jest": "jest --no-cache",
    "test": "cross-env NODE_ENV=test jest --no-cache --verbose --runInBand '-t' 'Delete123' ",
    "test-blog": "cross-env NODE_ENV=test jest --no-cache --verbose --runInBand '-t' 'dnwuie7'",
    "test-user": "cross-env NODE_ENV=test jest --no-cache --verbose --runInBand '-t' 'User'"
  },
  "author": "IB",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "cross-env": "^7.0.2",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.7.14",
    "mongoose-unique-validator": "^2.0.3"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "jest": "^25.4.0",
    "nodemon": "^2.0.1",
    "supertest": "^4.0.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
